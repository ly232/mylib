//bst.h
#ifndef _bst_h
#define _bst_h
#include "common.h"
namespace datastruct{
typedef struct node node;
template <typename T>
class bst
{
public:
  bst():root(NULL)
  {
    
  };
  ~bst()
  {

  };
  const bst& operator=(const bst& rhs)
  {
    return rhs;
  };
  bst(const bst&)
  {

  };
  int height()
  {
    return -1;
  };

  STATUS insert(const std::string& key, const T& value)
  {
    return FAIL;
  };
  
  STATUS remove(const std::string& key)
  {
    return FAIL;
  };
private:
  struct node
  {
    std::string key;
    T value;
    node* left, right;
  };
  node* root;
};
}//end namespace datastruct
#endif
